scorers:
  string_statistics: {}
  ribonn:
    # Paths are resolved from the working directory. Eval commands are run from
    # the repository root, so this points into the RiboNN submodule.
    weights_folder: RiboNN/models/human
    top_k_models_to_use: 5
    # batch_size: 32
  # utrlm: {}
  # rnafold: {}
